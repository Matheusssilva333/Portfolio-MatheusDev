import React from "react";
import { Project } from "../types";
const Projects: React.FC<{projects:Project[]}> = ({projects}) => <section id="projetos" className="relative py-28 md:py-36"><div className="section-shell"><div className="flex flex-col md:flex-row md:items-end justify-between gap-8 mb-14"><div><span className="section-kicker">Projetos</span><h2 className="section-title">Tecnologia com <span className="text-primary-gradient">propósito.</span></h2></div><p className="section-copy mt-0 max-w-md">Projetos que demonstram capacidade de construir, automatizar e transformar conhecimento em soluções concretas.</p></div><div className="grid md:grid-cols-2 gap-5">{projects.map((p,i)=>{const soon=p.title.includes("Em Desenvolvimento");return <article key={p.id} className="glass-morphism rounded-3xl overflow-hidden hover-lift group"><div className="aspect-[16/9] relative overflow-hidden"><img src={p.imageUrl} alt={p.title} loading="lazy" className={`w-full h-full object-cover transition duration-700 ${soon?'grayscale opacity-20':'grayscale opacity-40 group-hover:grayscale-0 group-hover:opacity-80 group-hover:scale-105'}`}/><div className="absolute inset-0 bg-gradient-to-t from-[#071018] via-transparent to-transparent"/><span className="absolute top-4 left-4 font-mono text-xs text-orange-400 bg-black/30 backdrop-blur px-3 py-2 rounded-lg border border-white/10">{String(i+1).padStart(2,"0")}</span>{soon&&<span className="absolute top-4 right-4 font-mono text-[9px] uppercase tracking-wider text-amber-300 border border-amber-400/20 bg-black/30 px-3 py-2 rounded-lg">Em desenvolvimento</span>}</div><div className="p-7"><h3 className={`text-xl font-extrabold ${soon?'text-slate-500':'text-white group-hover:text-orange-300'} transition-colors`}>{p.title}</h3><p className="mt-3 text-sm leading-7 text-slate-400">{p.description}</p><div className="flex flex-wrap gap-2 mt-6">{p.tags.map(t=><span key={t} className="font-mono text-[9px] uppercase tracking-wider text-slate-500 border border-white/5 rounded px-2.5 py-1.5">{t}</span>)}</div>{p.link&&!soon&&<a href={p.link} target="_blank" rel="noopener noreferrer" className="mt-7 inline-flex text-[10px] font-bold uppercase tracking-[.2em] text-orange-400 hover:text-orange-300">Abrir projeto <span className="ml-2">↗</span></a>}</div></article>})}</div><div className="mt-10 flex justify-end"><a href="https://github.com/Matheusssilva333" target="_blank" rel="noopener noreferrer" className="btn-outline rounded-lg px-5 py-3 text-[10px] font-bold uppercase tracking-[.18em]">Todos os repositórios ↗</a></div></div></section>;
export default React.memo(Projects);
